<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit your chat</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <p class="main-text"><u>Editing your chat : <%= chat._id %></u></p>
    <p class="main-text">
      This chat was sent from <b><%= chat.from %></b> to <b><%= chat.to %></b>
    </p>
    <form method="POST" action="/chats/<%= chat._id %>?_method=PUT">
      <!-- we use npm i method-override to use put method in html -->
      <textarea name="message" rows="7" cols="30" class="text-msg"><%= chat.message %></textarea>
      <br />
      <button class="edit-msg">Edit</button>
    </form>
  </body>
</html>